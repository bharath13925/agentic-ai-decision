# python-microservice/requirements.txt
# Install: pip install -r requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1

# Data & ML
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
xgboost==2.0.3
lightgbm==4.3.0
shap==0.45.1

# Groq LLM (replaces Ollama for production)
groq==0.9.0

# Vector search (optional — falls back to cosine if not installed)
faiss-cpu==1.8.0

# Ollama (optional — only for local embedding with nomic-embed-text)
# ollama==0.2.1

# HTTP client
httpx==0.27.0
requests==2.32.3